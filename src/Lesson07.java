import java.util.Arrays;

public class Lesson07 {
    public static void main(String[] args) {
        // Unidimensional Arrays

        // Arrays - if you don't know the items (Old version)
//        int[] numbers = new int[5];
//        numbers[0] = 1;
//        numbers[1] = 2;
//        System.out.println(Arrays.toString(numbers));

        //Arrays - if you know the items (New version)
//        int[] numbers = {2, 1, 5, 1, 4};
//        System.out.println(numbers.length);

//        System.out.println(Arrays.toString(numbers));

        // Arrays (New version) - using sort method
//        int[] numbers = {2, 3, 5, 1, 4};
//        Arrays.sort(numbers);
//        System.out.println(Arrays.toString(numbers));

        // Boolean type (Old version)
//        boolean[] message = new boolean[5];
//        message[0] = true;
//        System.out.println(Arrays.toString(message));

//        // Boolean type (New version)
//        boolean[] message = {true, false, true, false, false};
//        System.out.println(Arrays.toString(message));

        // Float type (Old version)
//        float[] numbers = new float[5];
//        numbers[0] = 3.4F;
//        System.out.println(Arrays.toString(numbers));

        // Float type (New version)
//        float[] numbers = {2.3F, 45.4F, 8.2F, 7};
//        System.out.println(Arrays.toString(numbers));

    }
}
